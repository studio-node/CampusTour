[Unit]
Description=CampusTour backend (staging)
After=network.target

[Service]
Type=simple
User=campustour
WorkingDirectory={{ app_dir }}/backend
EnvironmentFile=/etc/campustour/backend.env
ExecStart=/usr/bin/npm start
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target

